import { Button } from "~/components/ui/button";

export const FeedbackButton = () => {
  return (
    <Button size="sm" variant="ghost" className="w-full underline rounded-none">
      Leave feedback
    </Button>
  );
};
